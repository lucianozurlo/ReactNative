import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    listContainer: {
        marginHorizontal: 20,
    },
    listTitle: {
        fontSize: 22,
        fontWeight: 'bold',
        marginBottom: 20,
        color: '#222'
    }
});